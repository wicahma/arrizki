import React from "react";
import Layout from "@/components/Layout";

const index = (props: any) => {
  return (
    <Layout className="flex" pageTitle="Pesanan">
      <div className="grow w-full">pesanan</div>
    </Layout>
  );
};

export default index;
