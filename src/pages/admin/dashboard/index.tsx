import React from "react";
import Layout from "@/components/Layout";

const index = (props: any) => {
  return (
    <Layout className="flex" pageTitle="Admin Dashboard">
      <div className="grow w-full p-10">dashboard</div>
    </Layout>
  );
};

export default index;
